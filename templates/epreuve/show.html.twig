{% extends 'base.html.twig' %}

{% block title %}Epreuve{% endblock %}

{% block body %}
    <h1>Epreuve</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ epreuve.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ epreuve.nom }}</td>
            </tr>
            <div class="d-flex gap-2 mt-3">
                <a class="btn btn-main btn-sm" href="{{ path('app_appartient_a_new') }}">
                    Associer à un sport
                </a>
                <a class="btn btn-main btn-sm" href="{{ path('app_concerne_new') }}">
                    Associer à une compétition
                </a>
            </div>

        </tbody>
    </table>

    <a href="{{ path('app_epreuve_index') }}">back to list</a>

    <a href="{{ path('app_epreuve_edit', {'id': epreuve.id}) }}">edit</a>

    {{ include('epreuve/_delete_form.html.twig') }}
{% endblock %}
