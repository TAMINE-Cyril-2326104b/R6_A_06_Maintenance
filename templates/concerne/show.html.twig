{% extends 'base.html.twig' %}

{% block title %}Concerne{% endblock %}

{% block body %}
    <h1>Concerne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ concerne.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_concerne_index') }}">back to list</a>

    <a href="{{ path('app_concerne_edit', {'id': concerne.id}) }}">edit</a>

    {{ include('concerne/_delete_form.html.twig') }}
{% endblock %}
