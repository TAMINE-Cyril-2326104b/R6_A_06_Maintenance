{% extends 'base.html.twig' %}

{% block title %}Concerne index{% endblock %}

{% block body %}
    <h1>Concerne index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for concerne in concernes %}
            <tr>
                <td>{{ concerne.id }}</td>
                <td>
                    <a href="{{ path('app_concerne_show', {'id': concerne.id}) }}">show</a>
                    <a href="{{ path('app_concerne_edit', {'id': concerne.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_concerne_new') }}">Create new</a>
{% endblock %}
