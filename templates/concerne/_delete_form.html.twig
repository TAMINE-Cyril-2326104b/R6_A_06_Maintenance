<form method="post" action="{{ path('app_concerne_delete', {'id': concerne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ concerne.id) }}">
    <button class="btn">Delete</button>
</form>
