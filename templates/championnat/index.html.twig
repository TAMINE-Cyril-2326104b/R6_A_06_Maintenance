{% extends 'base.html.twig' %}

{% block title %}Championnat index{% endblock %}

{% block body %}
    <section class="container">
    <h1>Championnat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for championnat in championnats %}
            <tr>
                <td>{{ championnat.id }}</td>
                <td>{{ championnat.nom }}</td>
                <td>
                    <a href="{{ path('app_championnat_show', {'id': championnat.id}) }}">show</a>
                    <a href="{{ path('app_championnat_edit', {'id': championnat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_championnat_new') }}">Create new</a>
    </section>
{% endblock %}
